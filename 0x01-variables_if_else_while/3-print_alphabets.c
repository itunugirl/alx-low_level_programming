#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int a = 97;
	int A = 65;
		while (a <= 122)
		{
			putchar(a);
			a = a + 1;
		}
		while (A <= 90)
	{
		putcharr(A);
		A = A + 1;
	}
		putcharr('\n');
		return (0);
	}
